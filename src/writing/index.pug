---
title: Writing
layout: layouts/base.pug
draft: true
---

h2 #{title}
// Filter dafts & sort by date ascending (newest first)
- let sorted = collections.writing.filter(({data}) => !data.draft).reverse()
for post in sorted
    - let dateObject = new Date(post.date)
    - let month = dateObject.toLocaleString('en-US', { month: "long" })
    - let dateString = `${month} ${dateObject.getUTCDate()}, ${dateObject.getFullYear()}`
    li
        a(href=post.url) #{post.data.title}
        span  (#{dateString})
